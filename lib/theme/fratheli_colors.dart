import 'package:flutter/material.dart';

class FratheliColors {
  // Paleta oficial do Frathéli Layout Premium.
  static const ink = Color(0xFF1D1713);
  static const espresso = Color(0xFF21150F);
  static const espresso2 = Color(0xFF38251A);
  static const cream = Color(0xFFF4EFE6);
  static const paper = Color(0xFFFDFBF7);
  static const gold = Color(0xFFC89A45);
  static const gold2 = Color(0xFFE2B85C);
  static const cherry = Color(0xFF7C2D20);
  static const muted = Color(0xFF746A61);

  // Nomes antigos mantidos para as telas administrativas existentes.
  static const bg = paper;
  static const bg2 = cream;
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = cream;
  static const border = Color(0x241D1713);
  static const text = ink;
  static const text2 = Color(0xFF5D534B);
  static const text3 = muted;
  static const textMuted = muted;
  static const brown = espresso2;
  static const green = Color(0xFF347357);
  static const danger = Color(0xFFD93025);
}
