import 'package:flutter/material.dart';

class FratheliColors {
  // Fundo “claro elegante” (bege/café com leite)
  static const bg = Color(0xFFF7F4EE);
  static const bg2 = Color(0xFFFDFBF8);

  // Superfícies / cards
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF2EDE4);

  // Bordas / divisórias
  static const border = Color(0x1A000000);

  // Textos
  static const text = Color(0xFF1B1B1B);
  static const text2 = Color(0xFF5B5F6B);
  static const text3 = Color(0xFF8B909C);
  static const textMuted  = Color(0xFF6B6F7B); // 👈 NOVO (subtítulos)

  // Marca (dourados Frathéli)
  static const gold  = Color(0xFFD4AF37); // principal
  static const gold2 = Color(0xFFB58A2D); // secundário (mais fechado)
  static const brown  = Color(0xFF5A3E2B); // 👈 NOVO (logo FRATHÉLI)

  // Estados / ações
  static const green  = Color(0xFF1E8E3E);
  static const danger = Color(0xFFD93025);
}
